---
phase: 01-build-quality-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - OPS-01
  - OPS-02
user_setup: []
must_haves:
  truths:
    - "`package.json` defines `check` (astro check) and `verify` (lint + check + build)."
    - "Deploy workflow runs ESLint and Astro check after `bun install` and before the build step; build keeps `NODE_OPTIONS` and nvm sourcing."
    - "`bun run check` and `bun run lint` are invocable the same way locally and in CI."
  artifacts:
    - path: package.json
      provides: "Scripts check, verify"
      contains: '"check"'
    - path: .github/workflows/deploy.yml
      provides: "Lint and astro check gates before build"
      contains: "bun run lint"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "package.json scripts"
      via: "bun run lint / bun run check / bun run build"
      pattern: "bun run (lint|check|build)"
---

<objective>
Add discoverable `check` and `verify` scripts to `package.json`, and extend the self-hosted deploy workflow so install is followed by ESLint and `astro check` before production build—enforcing the same quality gates as local development.

Purpose: Satisfies OPS-01 (build remains the CI capstone) and OPS-02 (lint + `@astrojs/check` enforced in pipeline). Wave 2 fixes the current ESLint failures so `bun run lint` exits 0.

Output: Updated `package.json` scripts; updated `.github/workflows/deploy.yml` with ordered steps.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-quality-baseline/01-RESEARCH.md

ESLint intentionally ignores `**/*.astro` per `eslint.config.mjs`; Astro templates use `astro check`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add check and verify scripts to package.json</name>
  <read_first>
    - `package.json` — entire `scripts` block
    - `.planning/phases/01-build-quality-baseline/01-RESEARCH.md` — “Scripts in package.json” / Commands table
  </read_first>
  <files>package.json</files>
  <action>
    In `package.json`, inside `"scripts"`, add exactly:

    - `"check": "astro check"` — runs `@astrojs/check` (default failing severity: error; hints do not fail).

    - `"verify": "bun run lint && bun run check && bun run build"` — single local/CI gate: ESLint (TS/JS only per flat config), then Astro check, then production build.

    Preserve existing script keys and ordering except inserting `check` and `verify` after `lint:fix` (or immediately after `lint` if you prefer alphabetical; keep `verify` last among the three new names so readers see lint → check → build order in the composite).

    Do not remove or rename `lint`, `lint:fix`, `build`, or other existing scripts.

    Rationale: OPS-02 needs both `eslint` and `astro check`; OPS-01 needs `astro build` to remain green—`verify` composes all three per phase instructions.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); console.assert(p.scripts.check==='astro check'); console.assert(p.scripts.verify==='bun run lint && bun run check && bun run build');"` exits 0
    - `bun run check` exits 0 (may print hints; must not fail on hints at default severity)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/truongdq/tx/GitHub/portfolio && node -e "const p=require('./package.json'); console.assert(p.scripts.check==='astro check'); console.assert(p.scripts.verify==='bun run lint && bun run check && bun run build');" && bun run check</automated>
  </verify>
  <done>`check` and `verify` exist with exact string values above; `bun run check` completes with exit code 0.</done>
</task>

<task type="auto">
  <name>Task 2: Gate deploy workflow with lint and astro check before build</name>
  <read_first>
    - `.github/workflows/deploy.yml` — full file
    - `package.json` — `scripts.check`, `scripts.lint`, `scripts.build`
  </read_first>
  <files>.github/workflows/deploy.yml</files>
  <action>
    Edit `.github/workflows/deploy.yml` only.

    After the step named `Install dependencies` (`bun install`) and **before** the step that runs `bun run build`:

    1. Add a step **ESLint** with `run: bun run lint`

    2. Add a step **Astro check** with `run: bun run check`

    3. Replace the current **Build** step body so it still runs `bun run build` with the same `NODE_OPTIONS: "--max-old-space-size=4096"` and the same nvm sourcing pattern (`export NVM_DIR=...`, `[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"`) as today—only the preceding steps change.

    Do not remove checkout, setup-node, setup-bun, or deploy/PM2 steps. Triggers stay `push` to `main` on `self-hosted`.

    Note: Until Wave 2 completes, `bun run lint` may fail CI; Wave 2 resolves ESLint errors.

    Optional: name the build step `Build` (keep clear separation from lint/check).
  </action>
  <acceptance_criteria>
    - `grep -n "bun run lint" .github/workflows/deploy.yml` matches at least one line
    - `grep -n "bun run check" .github/workflows/deploy.yml` matches at least one line
    - `grep -n "bun run build" .github/workflows/deploy.yml` matches at least one line
    - In the file order, `Install dependencies` (or `bun install`) appears before `bun run lint`, and `bun run build` appears after `bun run check`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/truongdq/tx/GitHub/portfolio && grep -q "bun run lint" .github/workflows/deploy.yml && grep -q "bun run check" .github/workflows/deploy.yml && python3 -c "s=open('.github/workflows/deploy.yml').read(); i=s.find('bun install'); l=s.find('bun run lint'); c=s.find('bun run check'); b=s.find('bun run build'); assert i >= 0 and l > i and c > l and b > c, (i,l,c,b)"</automated>
  </verify>
  <done>Workflow runs `bun run lint`, then `bun run check`, then nvm-sourced `bun run build` with existing memory env.</done>
</task>

</tasks>

<verification>
After Wave 2: `bun run verify` exits 0 end-to-end. After this plan alone: `bun run check` passes; `bun run lint` may still fail until `01-02-PLAN.md` executes.
</verification>

<success_criteria>
Scripts are documented in-repo via `package.json`; CI enforces lint and Astro check before build; requirement traceability OPS-01/OPS-02 for pipeline wiring is satisfied pending green lint in Wave 2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-quality-baseline/01-01-SUMMARY.md`
</output>
