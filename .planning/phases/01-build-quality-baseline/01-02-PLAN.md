---
phase: 01-build-quality-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/index-content.ts
  - src/lib/certificate-generator.ts
  - src/pages/og/[...path].png.ts
  - src/lib/skill-tree-data.ts
  - src/components/RoadmapVoting.client.tsx
  - src/components/SkillTreeAdmin.client.tsx
  - src/components/TerminalTakeover.client.tsx
  - src/components/ReadingProgress.tsx
autonomous: true
requirements:
  - OPS-01
  - OPS-02
user_setup: []
must_haves:
  truths:
    - "`bun run lint` exits 0 with `--max-warnings 0` (16 errors cleared)."
    - "`bun run verify` completes (lint + check + build) after Wave 1 scripts exist."
  artifacts:
    - path: scripts/index-content.ts
      provides: "prefer-const, ts-expect-error, unused catch fixed"
    - path: src/components/ReadingProgress.tsx
      provides: "No setState in effect for visibility (react-hooks/set-state-in-effect)"
  key_links:
    - from: "ESLint"
      to: "src/components/ReadingProgress.tsx"
      via: "Derived visibility without synchronous setState in useEffect"
      pattern: "setIsVisible|useEffect"
---

<objective>
Clear all current ESLint **errors** (16) reported by `bun run lint` with minimal behavior change: mechanical fixes (imports, `prefer-const`, `@ts-expect-error`, unused bindings) and a small refactor of `ReadingProgress` to satisfy `react-hooks/set-state-in-effect`.

Purpose: Meets OPS-02 (`eslint` + `@astrojs/check` baseline); unblocks `bun run verify` for OPS-01.

Output: Eight source files updated; `bun run lint` exit code 0.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-build-quality-baseline/01-RESEARCH.md
@eslint.config.mjs

Known error inventory (2026-04-12): `scripts/index-content.ts` (3), `ReadingProgress.tsx` (1), `RoadmapVoting.client.tsx` (7 unused Lucide imports), `SkillTreeAdmin.client.tsx` (1), `TerminalTakeover.client.tsx` (1), `certificate-generator.ts` (1), `skill-tree-data.ts` (1), `og/[...path].png.ts` (1).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical ESLint fixes (scripts, lib, OG route, components)</name>
  <read_first>
    - `scripts/index-content.ts` — lines 20–100
    - `src/lib/certificate-generator.ts` — lines 20–28
    - `src/pages/og/[...path].png.ts` — lines 30–40
    - `src/lib/skill-tree-data.ts` — line 1
    - `src/components/RoadmapVoting.client.tsx` — lucide-react import block (lines 5–26)
    - `src/components/SkillTreeAdmin.client.tsx` — state around line 50
    - `src/components/TerminalTakeover.client.tsx` — line 4
  </read_first>
  <files>scripts/index-content.ts, src/lib/certificate-generator.ts, src/pages/og/[...path].png.ts, src/lib/skill-tree-data.ts, src/components/RoadmapVoting.client.tsx, src/components/SkillTreeAdmin.client.tsx, src/components/TerminalTakeover.client.tsx</files>
  <action>
    Apply these **exact** edits (minimal behavior change):

    1. **`scripts/index-content.ts`**
       - Line ~27: change `let filePath` to `const filePath` (single assignment; satisfies `prefer-const`).
       - Replace `// @ts-ignore` before `embedder(text, ...)` with `// @ts-expect-error Xenova pipeline output typing` (or one short reason) per `@typescript-eslint/ban-ts-comment`.
       - In `catch (e)` (~96), rename to `catch (_e)` or `catch {` if target allows — use `catch (_e)` for compatibility.

    2. **`src/lib/certificate-generator.ts`**
       - Replace `// @ts-ignore` above the `satori(` first argument with `// @ts-expect-error Satori intrinsic element object` (or equivalent short description).

    3. **`src/pages/og/[...path].png.ts`**
       - Same as (2) for the `satori(` call’s `// @ts-ignore`.

    4. **`src/lib/skill-tree-data.ts`**
       - Change `import type { SkillTreeData, SkillNode }` to import only `SkillTreeData` (remove unused `SkillNode`).

    5. **`src/components/RoadmapVoting.client.tsx`**
       - Remove unused Lucide imports: `TrendingUp`, `Award`, `Zap`, `Plus`, `Filter`, `Heart`, `GitPullRequest` only—keep every name that is still referenced in JSX/JS.

    6. **`src/components/SkillTreeAdmin.client.tsx`**
       - Rename unused state tuple to `const [_isLoading, setIsLoading] = useState(true);` per `varsIgnorePattern` `^_`.

    7. **`src/components/TerminalTakeover.client.tsx`**
       - Change `import { motion, AnimatePresence }` to `import { motion }` only if `AnimatePresence` is unused (confirmed: only import line references it).

    After edits, run `bun run lint` and fix any remaining issues in these files only before marking task done.
  </action>
  <acceptance_criteria>
    - Scoped ESLint on the seven files exits 0: `bunx eslint scripts/index-content.ts src/lib/certificate-generator.ts "src/pages/og/[...path].png.ts" src/lib/skill-tree-data.ts src/components/RoadmapVoting.client.tsx src/components/SkillTreeAdmin.client.tsx src/components/TerminalTakeover.client.tsx --ext .js,.mjs,.cjs,.ts,.tsx --max-warnings 0`
    - `grep -n "@ts-ignore" scripts/index-content.ts src/lib/certificate-generator.ts "src/pages/og/[...path].png.ts" || true` produces no lines (exit 1 from grep means no `@ts-ignore` remains—executor verifies no output)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/truongdq/tx/GitHub/portfolio && bunx eslint scripts/index-content.ts src/lib/certificate-generator.ts "src/pages/og/[...path].png.ts" src/lib/skill-tree-data.ts src/components/RoadmapVoting.client.tsx src/components/SkillTreeAdmin.client.tsx src/components/TerminalTakeover.client.tsx --ext .js,.mjs,.cjs,.ts,.tsx --max-warnings 0</automated>
  </verify>
  <done>All listed files except `ReadingProgress.tsx` lint clean; `@ts-ignore` removed from touched files.</done>
</task>

<task type="auto">
  <name>Task 2: ReadingProgress — remove synchronous setState in useEffect</name>
  <read_first>
    - `src/components/ReadingProgress.tsx` — full file
    - `eslint-plugin-react-hooks` rule `react-hooks/set-state-in-effect` (effect should not call setState synchronously for derived UI flags)
  </read_first>
  <files>src/components/ReadingProgress.tsx</files>
  <action>
    Refactor `ReadingProgress` so visibility is **not** set via `setIsVisible` inside `useEffect` with an empty dependency array.

    **Minimal approach (preserve prior behavior):** The previous effect only read `window.location.pathname` once on mount (deps `[]`). Replace with a `useState` **lazy initializer**:

    - `const [isVisible] = useState(() => { if (typeof window === 'undefined') return false; const path = window.location.pathname; const isPostPage = path.includes('/blog/') || path.includes('/journal/'); const isIndexPage = path === '/blog/' || path === '/journal/'; return isPostPage && !isIndexPage; });`

    - Remove the `useEffect` that only called `setIsVisible`, and remove `setIsVisible` / `useEffect` imports if unused.

    Keep `useScroll` / `useSpring` / `motion.div` behavior unchanged. Do not add new effects unless strictly required.

    Rationale: Visibility is derived from initial URL; original code did not update on client navigation (empty deps)—lazy init matches that contract and satisfies the rule.
  </action>
  <acceptance_criteria>
    - `bun run lint 2>&1 | grep -c "ReadingProgress"` is 0 (no ESLint output for that file)
    - `grep -n "useEffect" src/components/ReadingProgress.tsx` returns exit 1 (no `useEffect` left) OR only if you must keep an effect for a documented external reason—not for this visibility flag
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/truongdq/tx/GitHub/portfolio && bun run verify</automated>
  </verify>
  <done>`bun run verify` exits 0 (lint + check + build); `ReadingProgress.tsx` has no `react-hooks/set-state-in-effect` violation.</done>
</task>

</tasks>

<verification>
Run in order: `bun run lint`, `bun run check`, `bun run verify` (or `bun run build` after lint/check). All must exit 0.
</verification>

<success_criteria>
OPS-02: `bun run lint` and `bun run check` pass; OPS-01: `bun run build` passes via `verify`. No new ESLint errors; no broad rule disables.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-quality-baseline/01-02-SUMMARY.md`
</output>
