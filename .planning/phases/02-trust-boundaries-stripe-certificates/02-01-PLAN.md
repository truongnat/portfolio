---
phase: 02-trust-boundaries-stripe-certificates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/pages/api/stripe/webhook.ts
  - src/env.d.ts
autonomous: true
requirements:
  - SEC-01
user_setup:
  - service: stripe
    why: "Webhook signature verification uses the official Stripe Node SDK."
    env_vars:
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard → Developers → Webhooks → endpoint → Signing secret"
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard → Developers → API keys (required to construct `Stripe` client for `webhooks.constructEvent`)"
must_haves:
  truths:
    - "The webhook handler never treats the request body as a Stripe event without passing `constructEvent`."
    - "Invalid or missing `stripe-signature` or bad payload returns 400 without running payment/certificate business logic."
    - "`payment_intent.succeeded` handling (logging, future DB work) runs only on a verified `Stripe.Event` object."
  artifacts:
    - path: package.json
      provides: "`stripe` runtime dependency"
      contains: '"stripe"'
    - path: src/pages/api/stripe/webhook.ts
      provides: "Verified Stripe webhook using raw body + constructEvent"
      contains: constructEvent
  key_links:
    - from: "src/pages/api/stripe/webhook.ts"
      to: "Stripe.webhooks.constructEvent"
      via: "raw `request.text()` body passed unchanged into constructEvent"
      pattern: "constructEvent\\("
---

<objective>
Add the official `stripe` package and replace insecure `JSON.parse` in the Stripe webhook route with `Stripe.webhooks.constructEvent` using the raw body, the `stripe-signature` header, and `STRIPE_WEBHOOK_SECRET`. Map signature verification failures to 400 responses; keep all event handling behind the verified `event` object.

Purpose: Satisfies **SEC-01** — attackers cannot forge webhook payloads without Stripe’s signing key.

Output: `package.json` + lockfile with `stripe`; hardened `webhook.ts`; typed env entries for Stripe secrets used by this route.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trust-boundaries-stripe-certificates/02-RESEARCH.md
@src/pages/api/stripe/webhook.ts
@src/pages/api/stripe/create-payment.ts

<interfaces>
Current webhook (to replace insecure parse):

```typescript
// src/pages/api/stripe/webhook.ts — POST handler uses request.text() and stripe-signature header today; inner parse must become constructEvent.
```

`create-payment.ts` documents `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` in comments — use the same names in `webhook.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stripe dependency</name>
  <read_first>
    - `package.json` — dependencies
    - `.planning/phases/02-trust-boundaries-stripe-certificates/02-RESEARCH.md` — Summary / Recommendations
  </read_first>
  <files>package.json, bun.lock</files>
  <action>
    Run `bun add stripe` from the repo root so `stripe` appears in `dependencies` with a current ^18-compatible range (per RESEARCH). Do not add unrelated packages.

    Rationale: `Stripe.webhooks.constructEvent` lives in the official SDK; the repo does not list `stripe` yet.
  </action>
  <acceptance_criteria>
    - `grep -q '"stripe"' package.json` exits 0
    - `bun.lock` contains an entry for `stripe`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/truongdq/tx/GitHub/portfolio && grep -q '"stripe"' package.json && grep -q 'stripe' bun.lock</automated>
  </verify>
  <done>`stripe` is declared in `package.json` and locked; install is reproducible.</done>
</task>

<task type="auto">
  <name>Task 2: Verify webhooks with constructEvent and typed env</name>
  <read_first>
    - `src/pages/api/stripe/webhook.ts` — full file
    - `src/env.d.ts` — ImportMetaEnv
  </read_first>
  <files>src/pages/api/stripe/webhook.ts, src/env.d.ts</files>
  <action>
    1. **Imports:** `import Stripe from 'stripe'` (or the package’s recommended default import for this version).

    2. **Secrets:** Read `import.meta.env.STRIPE_WEBHOOK_SECRET` and `import.meta.env.STRIPE_SECRET_KEY`. If `STRIPE_WEBHOOK_SECRET` is missing/empty, return **400** with a short body (e.g. `Webhook not configured`) and do not parse the body as JSON. Instantiate `new Stripe(stripeSecretKey, { apiVersion: use a pinned stable string matching the installed SDK’s default export or docs })` only when both webhook secret and secret key are present; if secret key is missing but webhook secret is set, return **503** or **400** with a clear server-misconfiguration message (choose one consistent status and document in file comment).

    3. **Verification:** After `const body = await request.text()` and `const signature = request.headers.get('stripe-signature')`, require `signature` — if missing, **400**. Call `stripe.webhooks.constructEvent(body, signature, webhookSecret)` inside try/catch. On `Stripe.errors.StripeSignatureVerificationError` (or equivalent type guard from the installed SDK), return **400** and log a single line server-side; **do not** echo Stripe internals to the client.

    4. **Business logic:** Remove `JSON.parse(body)` for event data. Use only the `event` returned from `constructEvent`. Keep existing `payment_intent.succeeded` branch logic but read `event.data.object` from the typed event (narrow with `event.type` check). No TODO that re-parses raw body for the same request.

    5. **Success response:** Keep `200` + `{ received: true }` JSON for successfully verified and handled requests (or Stripe’s expected acknowledgment pattern).

    6. **`src/env.d.ts`:** Add `readonly STRIPE_SECRET_KEY: string` and `readonly STRIPE_WEBHOOK_SECRET: string` to `ImportMetaEnv` so the route type-checks (use empty string union if Astro pattern prefers optional — match existing project style).

    7. **File comment:** Update the top comment block to list `STRIPE_WEBHOOK_SECRET` and `STRIPE_SECRET_KEY` accurately.

    Avoid: parsing `body` with `JSON.parse` before `constructEvent`; logging full raw body in production logs.
  </action>
  <acceptance_criteria>
    - `grep -q 'constructEvent' src/pages/api/stripe/webhook.ts` exits 0
    - `grep -q 'JSON.parse(body)' src/pages/api/stripe/webhook.ts` exits 1 (must not remain)
    - `grep -q 'StripeSignatureVerificationError\\|Stripe\\.errors' src/pages/api/stripe/webhook.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/truongdq/tx/GitHub/portfolio && grep -q 'constructEvent' src/pages/api/stripe/webhook.ts && ! grep -q 'JSON.parse(body)' src/pages/api/stripe/webhook.ts && (grep -q 'StripeSignatureVerificationError' src/pages/api/stripe/webhook.ts || grep -q 'Stripe.errors' src/pages/api/stripe/webhook.ts) && bun run lint && bun run check</automated>
  </verify>
  <done>Webhook verifies Stripe signatures before any event handling; verification errors yield 400 without running business logic on forged bodies.</done>
</task>

</tasks>

<verification>
`bun run lint && bun run check` pass. Manual (optional): `stripe listen --forward-to localhost:4321/api/stripe/webhook` and trigger `payment_intent.succeeded` — endpoint returns 200 when signing secret matches.
</verification>

<success_criteria>
**SEC-01** is met: webhook signature verification uses `STRIPE_WEBHOOK_SECRET` and `constructEvent` on the raw body; invalid signatures never drive application logic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-trust-boundaries-stripe-certificates/02-01-SUMMARY.md`
</output>
