---
phase: 02-trust-boundaries-stripe-certificates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/api/certificate.ts
  - src/env.d.ts
autonomous: true
requirements:
  - SEC-02
user_setup:
  - service: certificate-hmac
    why: "Prevents minting certificates for arbitrary query strings without a server-issued signature."
    env_vars:
      - name: CERTIFICATE_SIGNING_SECRET
        source: "Generate a long random string (e.g. `openssl rand -hex 32`); store in deployment secrets only."
must_haves:
  truths:
    - "Requests without a valid HMAC over the certificate fields cannot obtain a PNG (401)."
    - "The HMAC uses a server-only secret; clients cannot forge valid signatures without that secret."
    - "Zod-validated fields are the same data covered by the signature (no extra unauthenticated fields)."
  artifacts:
    - path: src/pages/api/certificate.ts
      provides: "HMAC gate before generateCertificatePng"
      contains: timingSafeEqual|timing-safe|createHmac
  key_links:
    - from: "src/pages/api/certificate.ts"
      to: "src/lib/certificate-generator.ts"
      via: "generateCertificatePng only after signature OK"
      pattern: "generateCertificatePng"
---

<objective>
Require an HMAC-based `sig` query parameter (hex) over canonical certificate fields using `CERTIFICATE_SIGNING_SECRET`. Reject missing, malformed, or incorrect signatures with **401** before calling `generateCertificatePng`. Document required env vars in the route’s file comment (no `.env.example` in repo today).

Purpose: Satisfies **SEC-02** — public callers cannot forge arbitrary donor/skill certificate images by guessing query params.

Output: Hardened `certificate.ts`; `CERTIFICATE_SIGNING_SECRET` in `src/env.d.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trust-boundaries-stripe-certificates/02-RESEARCH.md
@src/pages/api/certificate.ts
@src/lib/certificate-generator.ts

<interfaces>
From `src/pages/api/certificate.ts` (current query schema):

```typescript
const querySchema = z.object({
  donatorName: z.string(),
  skillName: z.string(),
  amount: z.string(),
  date: z.string(),
  certificateId: z.string(),
});
```

From `src/lib/certificate-generator.ts`:

```typescript
export async function generateCertificatePng(params: {
  donatorName: string;
  skillName: string;
  amount: number;
  date: string;
  certificateId: string;
}): Promise<Buffer | Uint8Array>;
```
(Exact return type as in repo — executor reads file.)

Signing must cover the same five fields; add required query param e.g. `sig` (hex HMAC-SHA256).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HMAC gate for certificate issuance</name>
  <read_first>
    - `src/pages/api/certificate.ts` — full file
    - `src/lib/certificate-generator.ts` — `generateCertificatePng` signature
    - `src/env.d.ts`
  </read_first>
  <files>src/pages/api/certificate.ts, src/env.d.ts</files>
  <action>
    1. **Env:** Read `import.meta.env.CERTIFICATE_SIGNING_SECRET`. If missing/empty, return **503** (or **401** if you prefer a single client-facing status — prefer **503** for “server not configured”) with a generic body; do not generate images without a secret.

    2. **Query schema:** Extend Zod schema with `sig: z.string().min(1)` (name it `sig` per RESEARCH). Parse all params including `sig` after collecting `Object.fromEntries(url.searchParams)`.

    3. **Canonical message:** Build a deterministic string from the five data fields **only** (exclude `sig`), in a fixed order documented in a comment — e.g. UTF-8 string: `donatorName\\nskillName\\namount\\ndate\\ncertificateId` with values taken from parsed strings **before** `Number(amount)` for the generator. Same order as current Zod object keys or explicit sorted keys; must be stable across requests.

    4. **Expected MAC:** `crypto.createHmac('sha256', CERTIFICATE_SIGNING_SECRET).update(canonical, 'utf8').digest('hex')` (use `node:crypto`).

    5. **Compare:** Use `timingSafeEqual` on equal-length `Buffer`s from the provided `sig` hex and expected hex. If lengths differ (invalid hex length), **401**. Mismatch → **401** with body like `Unauthorized` (generic). Do not leak which field failed.

    6. **Only then** call `generateCertificatePng` with `amount: Number(validated.amount)` as today.

    7. **Zod errors:** Keep **400** for malformed params; **401** only for bad/missing signature after schema passes (or missing `sig` — can be 401 if `sig` required).

    8. **`src/env.d.ts`:** Add `readonly CERTIFICATE_SIGNING_SECRET: string`.

    9. **File comment:** Document `CERTIFICATE_SIGNING_SECRET` and that valid `sig` must be produced server-side (e.g. after verified payment or internal tool) using the same canonical format.

    Optional: export a small `buildCertificateCanonicalQuery(...)` in the same file for clarity — not a new file unless needed for tests.

    Do not weaken cache headers in this task unless required for correctness (immutable cache is OK for signed URLs).
  </action>
  <acceptance_criteria>
    - `grep -q 'createHmac\\|timingSafeEqual' src/pages/api/certificate.ts` exits 0
    - `grep -q 'CERTIFICATE_SIGNING_SECRET' src/pages/api/certificate.ts` exits 0
    - `grep -q '401' src/pages/api/certificate.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/truongdq/tx/GitHub/portfolio && grep -E -q 'createHmac|timingSafeEqual' src/pages/api/certificate.ts && grep -q 'CERTIFICATE_SIGNING_SECRET' src/pages/api/certificate.ts && grep -q '401' src/pages/api/certificate.ts && bun run lint && bun run check</automated>
  </verify>
  <done>Unsigned or tampered certificate requests receive 401; only HMAC-valid requests reach PNG generation.</done>
</task>

</tasks>

<verification>
`bun run lint && bun run check` pass. Manual: request `/api/certificate?...` without `sig` → 401; with wrong `sig` → 401; with HMAC computed using the documented canonical string and secret → 200 PNG.
</verification>

<success_criteria>
**SEC-02** is met: certificate PNGs are not mintable for arbitrary donor/skill data without a valid server-side HMAC (`CERTIFICATE_SIGNING_SECRET`).
</success_criteria>

<output>
After completion, create `.planning/phases/02-trust-boundaries-stripe-certificates/02-02-SUMMARY.md`
</output>
