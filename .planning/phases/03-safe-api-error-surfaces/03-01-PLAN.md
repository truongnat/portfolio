---
phase: 03-safe-api-error-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api-error-response.ts
  - src/pages/api/search.ts
autonomous: true
requirements:
  - SEC-03
user_setup: []
must_haves:
  truths:
    - "Search API 500 responses never include raw Error.message or stack text in JSON."
    - "Unexpected errors in search are logged server-side with full detail."
  artifacts:
    - path: src/pages/api/search.ts
      provides: "Generic JSON error on search failure"
    - path: src/lib/api-error-response.ts
      provides: "Shared log + JSON Response helpers"
  key_links:
    - from: "src/pages/api/search.ts"
      to: "src/lib/api-error-response.ts"
      via: "catch block uses logApiError + generic body"
---

<objective>
Introduce a small shared helper for logging unexpected API errors and returning **generic JSON** to clients. Update **`src/pages/api/search.ts`** so the `catch` path never returns `(error as Error).message` — only a stable user-safe message — while logging the real error server-side.

Purpose: Satisfies the **search** half of **SEC-03** (per ROADMAP success criterion 1).

Output: New `src/lib/api-error-response.ts`; hardened `search.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-safe-api-error-surfaces/03-CONTEXT.md
@.planning/phases/03-safe-api-error-surfaces/03-RESEARCH.md
@src/pages/api/search.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add api-error-response helper</name>
  <read_first>
    - `src/lib/api-utils.ts` — existing patterns (if any)
  </read_first>
  <files>src/lib/api-error-response.ts</files>
  <action>
    Create `src/lib/api-error-response.ts` exporting at minimum:
    - `logApiError(context: string, error: unknown): void` — logs with a clear prefix; include `error` as second arg so Node prints message/stack when present.
    - `jsonErrorResponse(status: number, body: Record<string, unknown>): Response` — `Content-Type: application/json`.

    Keep the module dependency-free beyond TypeScript/standard lib. No new npm packages.

    Optional: `SEARCH_UNAVAILABLE_BODY` constant for the exact JSON shape used by search 500s (single stable `error` string).
  </action>
  <acceptance_criteria>
    - File exists and is imported by `search.ts` after Task 2
    - `bun run lint` passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/truongdq/tx/GitHub/portfolio && bun run lint && bun run check</automated>
  </verify>
  <done>Shared helpers available for search and follow-up routes.</done>
</task>

<task type="auto">
  <name>Task 2: Harden search.ts catch block</name>
  <read_first>
    - `src/pages/api/search.ts` — full file
    - `src/lib/api-error-response.ts`
  </read_first>
  <files>src/pages/api/search.ts</files>
  <action>
    Replace the catch block that returns `(error as Error).message` with:
    1. `logApiError('[Search API]', error)` (or similar context string).
    2. Return **500** with JSON body `{ "error": "Search is temporarily unavailable" }` (exact wording) via `jsonErrorResponse`.

    Do not expose internal diagnostics in the response body or headers.

    Verify with grep: `src/pages/api/search.ts` must not contain `Error).message` or `.message` in the catch response path.
  </action>
  <acceptance_criteria>
    - `! grep -q '(error as Error).message' src/pages/api/search.ts`
    - Search success path unchanged (200 + array / empty array)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/truongdq/tx/GitHub/portfolio && ! grep -q '(error as Error).message' src/pages/api/search.ts && bun run lint && bun run check</automated>
  </verify>
  <done>Search failures are generic to clients, detailed in logs.</done>
</task>

</tasks>

<verification>
`bun run lint && bun run check && bun run build`. Optional manual: force an error (rename `data/lancedb` temporarily) and confirm JSON body is the generic string while console shows real error.
</verification>

<success_criteria>
**SEC-03** search criterion: clients never receive implementation-sensitive error text from `GET /api/search`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-safe-api-error-surfaces/03-01-SUMMARY.md`
</output>
