---
phase: 03-safe-api-error-surfaces
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01-PLAN.md
files_modified:
  - src/pages/api/certificate-issue.ts
autonomous: true
requirements:
  - SEC-03
user_setup: []
must_haves:
  truths:
    - "No API response body names internal env vars or implies missing signing secrets to anonymous clients."
    - "503/500 from certificate-issue uses generic outward message; details only in logs."
  artifacts:
    - path: src/pages/api/certificate-issue.ts
      provides: "Generic error JSON on misconfiguration"
      pattern: "logApiError|jsonErrorResponse"
  key_links:
    - from: "src/pages/api/certificate-issue.ts"
      to: "src/lib/api-error-response.ts"
      via: "reuse helpers from 03-01"
---

<objective>
Apply the same **generic-error** pattern to **`POST /api/certificate-issue`**: when `CERTIFICATE_SIGNING_SECRET` is missing or JSON parsing fails unexpectedly, return **non-diagnostic** JSON and log details server-side. Optionally re-scan **`src/pages/api`** for any remaining `Error.message` in response bodies and fix in this task if found.

Purpose: Satisfies the **“other sensitive API routes / peers”** half of **SEC-03** (ROADMAP success criterion 2), starting with the dev issuance route called out in research.

Output: Hardened `certificate-issue.ts`; grep-clean peer audit note in SUMMARY if applicable.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-safe-api-error-surfaces/03-RESEARCH.md
@src/pages/api/certificate-issue.ts
@src/lib/api-error-response.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generic errors for certificate-issue</name>
  <read_first>
    - `src/pages/api/certificate-issue.ts`
    - `src/lib/api-error-response.ts` (from plan 03-01)
  </read_first>
  <files>src/pages/api/certificate-issue.ts</files>
  <action>
    1. Import `logApiError` and `jsonErrorResponse` (or equivalent) from `src/lib/api-error-response.ts`.

    2. When `CERTIFICATE_SIGNING_SECRET` is missing/empty: **log** a clear server-only message (may mention env name in **log only**). Return **503** with a **generic** JSON body, e.g. `{ "error": "Service unavailable" }` — do **not** include the substring `CERTIFICATE_SIGNING_SECRET` in the response body.

    3. For non-Zod thrown errors in `catch` (if any): log + generic 500 JSON, not raw `message` to client.

    4. Keep **404** for `import.meta.env.PROD` unchanged.

    5. Run repo grep: `rg 'Error\\).message|error\\.message' src/pages/api` — ensure no new leaks; fix any stragglers in `src/pages/api` only if trivial (same helper).
  </action>
  <acceptance_criteria>
    - 503/500 JSON bodies do not contain the literal env var name `CERTIFICATE_SIGNING_SECRET` or other deployment hints; operators see detail in **logs only**.
    - `import.meta.env.CERTIFICATE_SIGNING_SECRET` may remain in code for reads; user-facing strings are generic.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/truongdq/tx/GitHub/portfolio && bun run lint && bun run check</automated>
  </verify>
  <done>Issuance route does not leak configuration in HTTP JSON bodies.</done>
</task>

</tasks>

<verification>
`bun run verify`. Manual: `POST /api/certificate-issue` without secret in dev → 503 body generic; server log indicates misconfiguration.
</verification>

<success_criteria>
**SEC-03** peer criterion: sensitive routes follow generic outward / detailed inward; certificate-issue aligned with search hardening.
</success_criteria>

<output>
After completion, create `.planning/phases/03-safe-api-error-surfaces/03-02-SUMMARY.md`
</output>
