---
phase: 04-data-layer-payment-honesty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/production-stub-guard.ts
  - src/pages/api/donations.ts
  - src/pages/api/roadmap/vote.ts
  - src/pages/api/learning-journey/sponsor.ts
  - src/pages/api/bug-bounty/submit.ts
  - src/pages/api/auction/create-bid.ts
  - src/pages/api/crypto/create-payment.ts
  - src/pages/api/terminal/execute.ts
  - src/pages/api/ai-thank-you/generate.ts
  - src/pages/api/stripe/create-payment.ts
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "In import.meta.env.PROD, listed stub POST routes do not return 200 with synthetic success for fake persistence."
    - "Stripe create-payment does not return mock PaymentIntent success in production when STRIPE_SECRET_KEY is missing."
  artifacts:
    - path: src/lib/production-stub-guard.ts
      provides: "Shared early-exit for production stub APIs"
---

<objective>
Add a small **`production-stub-guard`** helper and call it at the start of each **non-persistent demo POST** route listed in CONCERNS, plus harden **`stripe/create-payment`** so production never returns **mock** success without Stripe keys.

Purpose: **DATA-01** — production behavior is honest about missing backends.

Output: New guard module; updated API routes; `bun run verify` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-data-layer-payment-honesty/04-CONTEXT.md
@.planning/phases/04-data-layer-payment-honesty/04-RESEARCH.md
@src/lib/api-error-response.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: production-stub-guard helper</name>
  <files>src/lib/production-stub-guard.ts</files>
  <action>
    Create `src/lib/production-stub-guard.ts` exporting e.g. `stubUnavailableInProduction(routeLabel: string): Response | null`.

    - If `import.meta.env.PROD` is true: log server-side (use `logApiError` from `@/lib/api-error-response`) with a clear message that a stub route was invoked; return `jsonErrorResponse(503, { success: false, error: 'This feature is not enabled in production.' })` (exact `error` string stable across routes).
    - If not production: return `null` so the handler continues.

    Import `jsonErrorResponse` + `logApiError` from existing helpers.
  </action>
  <verify>
    <automated>bun run lint && bun run check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Gate stub POST handlers</name>
  <files>
    src/pages/api/donations.ts
    src/pages/api/roadmap/vote.ts
    src/pages/api/learning-journey/sponsor.ts
    src/pages/api/bug-bounty/submit.ts
    src/pages/api/auction/create-bid.ts
    src/pages/api/crypto/create-payment.ts
    src/pages/api/terminal/execute.ts
    src/pages/api/ai-thank-you/generate.ts
  </files>
  <action>
    At the top of each **POST** handler (after exports), immediately after entering the try or as first lines:

    `const blocked = stubUnavailableInProduction('/api/...'); if (blocked) return blocked;`

    Use the actual route path string for `routeLabel` for grepability.

    Preserve existing Zod validation and dev behavior below the guard.
  </action>
  <verify>
    <automated>bun run lint && bun run check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Stripe create-payment production honesty</name>
  <files>src/pages/api/stripe/create-payment.ts</files>
  <action>
    When `!stripeSecretKey`:
    - If **`import.meta.env.PROD`**: return **503** JSON with same generic shape as stub guard (`success: false`, stable `error` message) — **not** `success: true` mock mode.
    - If **development**: keep existing mock **200** behavior for local UX.

    When `stripeSecretKey` is present, leave real Stripe flow unchanged.
  </action>
  <verify>
    <automated>bun run lint && bun run check && bun run build</automated>
  </verify>
</task>

</tasks>

<verification>
`bun run verify`. Optional: build with production mode and `curl -X POST` stub routes → expect 503.
</verification>

<success_criteria>
**DATA-01** satisfied for listed routes: no misleading synthetic success in production for demo stubs; Stripe mock path disabled in production.
</success_criteria>

<output>
After completion, `.planning/phases/04-data-layer-payment-honesty/04-01-SUMMARY.md`
</output>
