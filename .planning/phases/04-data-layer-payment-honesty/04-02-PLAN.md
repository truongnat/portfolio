---
phase: 04-data-layer-payment-honesty
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01-PLAN.md
files_modified:
  - src/lib/db/index.ts
  - src/lib/db/README.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements:
  - DATA-02
user_setup: []
must_haves:
  truths:
    - "No executable D1 query module remains at src/lib/db/index.ts implying live bindings."
    - "schema.sql remains discoverable as reference DDL."
  artifacts:
    - path: src/lib/db/README.md
      provides: "Explains reference-only schema and removal of TS helpers"
---

<objective>
Remove unused **`src/lib/db/index.ts`** (dead Cloudflare D1-style helpers). Keep **`src/lib/db/schema.sql`**. Add **`src/lib/db/README.md`** describing that the SQL file is a **reference schema** not bound in the current `@astrojs/node` deployment. Light-touch updates to **`.planning/codebase`** docs that still describe `index.ts` as active.

Purpose: **DATA-02** — no drift from an unwired database module.

Output: README + deleted index; verify passes; docs consistent.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/lib/db/schema.sql
@.planning/codebase/INTEGRATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove db index.ts and document</name>
  <files>src/lib/db/index.ts, src/lib/db/README.md</files>
  <action>
    1. Delete `src/lib/db/index.ts`.

    2. Create `src/lib/db/README.md` with:
       - Purpose: reference DDL for a future D1 or SQL-backed deployment
       - Current deploy: `@astrojs/node`; no `env.DONATE_DB` binding in this repo
       - `schema.sql` is the source of truth for table definitions; TypeScript helpers were removed in Phase 4 to avoid implying live persistence

    3. Confirm `rg "from ['\"]@/lib/db['\"]|from ['\"].*lib/db"` over `src/` returns no broken imports (should be none).
  </action>
  <verify>
    <automated>bun run lint && bun run check && bun run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Align planning codebase docs</name>
  <files>.planning/codebase/INTEGRATIONS.md, .planning/codebase/ARCHITECTURE.md</files>
  <action>
    Update only the sentences that state `src/lib/db/index.ts` is an active/wired module. Replace with language matching README: reference schema + helpers removed, not bound in current stack.

    Skip unrelated doc rewrites.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

</tasks>

<verification>
`bun run verify`; `test ! -f src/lib/db/index.ts`
</verification>

<success_criteria>
**DATA-02** satisfied: dead module removed; reference DDL retained and documented.
</success_criteria>

<output>
After completion, `.planning/phases/04-data-layer-payment-honesty/04-02-SUMMARY.md`
</output>
