---
phase: 05-publishing-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/content/schemas.ts
  - scripts/validate-content.ts
  - package.json
autonomous: true
requirements:
  - CONT-01
user_setup: []
must_haves:
  truths:
    - "Blog, journal, and course Markdown/MDX frontmatter validates against the same Zod schemas used by Astro Content Collections."
    - "`bun run verify` (or an explicit content step wired into it) runs the validator."
  artifacts:
    - path: scripts/validate-content.ts
      provides: "Standalone frontmatter validation for all three collections"
    - path: src/content/schemas.ts
      provides: "Shared Zod schemas for content.config and script"
---

<objective>
Establish a **single source of truth** for Content Collection schemas and add a **fast validation script** so drift between frontmatter and Zod is caught in **`bun run verify`**, not only indirectly via full build.

Purpose: **CONT-01** — no silent schema/build regressions across blog, journal, and courses.

Output: `src/content/schemas.ts`; `content.config.ts` imports schemas; `scripts/validate-content.ts`; `package.json` script + verify hook; `bun run verify` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-publishing-contact/05-CONTEXT.md
@.planning/phases/05-publishing-contact/05-RESEARCH.md
@src/content.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared Zod schemas</name>
  <files>src/content/schemas.ts, src/content.config.ts</files>
  <action>
    1. Create `src/content/schemas.ts` exporting Zod objects equivalent to current `blog`, `journal`, and `courses` schemas in `content.config.ts` (same fields, defaults, enums).

    2. Update `content.config.ts` to import those schemas into `defineCollection({ schema: ... })`.

    3. Run `bun run check` to ensure types still align.
  </action>
  <verify>
    <automated>bun run check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: validate-content script + verify hook</name>
  <files>scripts/validate-content.ts, package.json</files>
  <action>
    1. Add `scripts/validate-content.ts` that:
       - Glob-matches the same paths as each collection (`src/content/blog`, `src/content/journal`, `src/content/courses`; exclude files starting with `_` if that matches loader behavior).
       - Uses `gray-matter` to parse frontmatter from each `.md`/`.mdx` file.
       - Runs the appropriate Zod schema `.safeParse` on `data`; on failure, print file path + issues and **exit 1**.

    2. Add npm script e.g. `"content:validate": "bun scripts/validate-content.ts"` and append it to **`verify`** after `check` (or before `build` — order: lint → check → **content:validate** → build).

    3. Run `bun run verify` green.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

</tasks>

<verification>
`bun run verify`; intentionally break one frontmatter field in a dev copy → script fails (spot-check optional).
</verification>
