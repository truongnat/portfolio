---
phase: 05-publishing-contact
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01-PLAN.md
files_modified:
  - src/components/ContactForm.client.tsx
  - src/pages/api/contact.ts
  - src/lib/telegram.ts
  - README.md
autonomous: true
requirements:
  - CONT-02
user_setup:
  - "For local contact testing: set TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID in environment for the Astro dev server (not PUBLIC_*)."
must_haves:
  truths:
    - "Contact form submits to POST /api/contact with JSON; Telegram is never called from client bundle code."
    - "Production/deploy docs mention TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID (server-only), not PUBLIC_TELEGRAM_* for delivery."
  artifacts:
    - path: src/components/ContactForm.client.tsx
      provides: "fetch-based submit to /api/contact"
---

<objective>
Wire the **contact form** to **`POST /api/contact`**, remove **client-side Telegram** (`PUBLIC_*` + `src/lib/telegram.ts` direct API calls), optionally align the API with **`logApiError`** for failures, and document **server** env vars for operators.

Purpose: **CONT-02** — messages deliver via Telegram when server credentials are configured, without exposing tokens to browsers.

Output: Updated `ContactForm.client.tsx`; delete or gut `telegram.ts`; small README/env note; `bun run verify` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-publishing-contact/05-CONTEXT.md
@src/pages/api/contact.ts
@src/lib/api-error-response.ts
@src/components/ContactForm.client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactForm → /api/contact</name>
  <files>src/components/ContactForm.client.tsx</files>
  <action>
    1. Replace `sendContactMessage` usage with `fetch('/api/contact', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name, email, message }) })`.

    2. Parse JSON response; treat `success: true` as success. On non-OK or `success: false`, show a **user-safe** message (use server `error` string where appropriate, or generic fallback — avoid surfacing stack traces).

    3. Preserve client-side localStorage rate-limit behavior.

    4. Remove import of `@/lib/telegram`.
  </action>
  <verify>
    <automated>bun run lint && bun run check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Remove client telegram module; harden API logging</name>
  <files>src/lib/telegram.ts, src/pages/api/contact.ts, README.md</files>
  <action>
    1. Delete `src/lib/telegram.ts` if nothing else imports it, or replace with a non-Telegram stub only if needed (prefer **delete**).

    2. In `contact.ts`: use `logApiError` from `@/lib/api-error-response` in `catch` and for Telegram HTTP failures (keep client JSON generic).

    3. Add a short **Environment** or **Contact (Telegram)** subsection to `README.md`: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` required for delivery; note that **`PUBLIC_TELEGRAM_*` must not be used for bot tokens** (removed from form path).

    4. Grep repo for `PUBLIC_TELEGRAM` and remove or update stale references.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

</tasks>

<verification>
`bun run verify`; manual: with env set, POST from UI or curl → 200 and Telegram message; without env → 500 with generic body, no secrets in response.
</verification>
