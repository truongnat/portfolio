---
phase: 06-domain-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - src/lib/config.ts
  - src/layouts/BaseLayout.astro
  - src/components/seo/Schema.astro
  - src/pages/blog/[...slug].astro
  - src/pages/og/[...path].png.ts
autonomous: true
requirements:
  - DOM-01
user_setup: []
must_haves:
  truths:
    - "astro.config.mjs site is https://truongsoftware.com"
    - "All former https://truongdq.com fallbacks in listed src files use the new canonical URL via a single shared constant or import"
  artifacts:
    - path: src/lib/config.ts (or src/lib/site.ts)
      provides: "CANONICAL_SITE_URL / seo.url aligned with Astro site"
---

<objective>
Switch **application** canonical URL to **`https://truongsoftware.com`**: Astro `site`, `seo.url`, layout/Schema/blog OG fallbacks, and OG PNG branding text — using **one exported canonical constant** to avoid future drift.

Purpose: **DOM-01** (application source path).

Output: Updated files above; `bun run verify` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-domain-cutover/06-CONTEXT.md
@.planning/phases/06-domain-cutover/06-RESEARCH.md
@astro.config.mjs
@src/lib/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonical constant + astro.config</name>
  <files>src/lib/config.ts, astro.config.mjs</files>
  <action>
    1. Add e.g. `export const CANONICAL_SITE_URL = 'https://truongsoftware.com'` (or equivalent name) next to `seo` in `src/lib/config.ts`; set `seo.url` to that constant.

    2. Set `site: 'https://truongsoftware.com'` in `astro.config.mjs` (must match).

    3. Prefer importing the constant in `.astro`/`.ts` files that need a fallback instead of duplicating string literals.
  </action>
  <verify>
    <automated>bun run check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Layouts, blog share URL, OG image</name>
  <files>src/layouts/BaseLayout.astro, src/components/seo/Schema.astro, src/pages/blog/[...slug].astro, src/pages/og/[...path].png.ts</files>
  <action>
    1. Replace `https://truongdq.com` fallbacks with `CANONICAL_SITE_URL` import from `@/lib/config` (or shared module).

    2. In `og/[...path].png.ts`, update visible branding text from `truongdq.com` to `truongsoftware.com` (or derive short host from constant).

    3. Ensure no `truongdq.com` remains under `src/` after grep.
  </action>
  <verify>
    <automated>bun run lint && bun run check</automated>
  </verify>
</task>

</tasks>

<verification>
`bun run verify`; `rg 'truongdq\.com' src/` returns no matches.
</verification>
