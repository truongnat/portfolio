---
phase: 06-domain-cutover
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01-PLAN.md
files_modified:
  - nginx.conf
  - PREVIEW_SETUP.md
  - docs/DOMAIN_MIGRATION.md
  - .planning/codebase/STACK.md
  - .cursor/rules/gsd-project.md
  - src/content/journal/2026-03-18-dry-kiss-yagni-technical-writing.md
autonomous: true
requirements:
  - DOM-02
user_setup:
  - "After merge: update DNS A records and TLS (certbot) for truongsoftware.com and preview subdomain per PREVIEW_SETUP.md"
must_haves:
  truths:
    - "nginx.conf and PREVIEW_SETUP.md reference truongsoftware.com hostnames (not truongdq.com)"
    - "docs/DOMAIN_MIGRATION.md exists and lists any remaining intentional legacy-domain mentions outside src/"
  artifacts:
    - path: docs/DOMAIN_MIGRATION.md
      provides: "Migration note for operators and archived-content exceptions"
---

<objective>
Update **deployment and documentation** to the new domain; add **`docs/DOMAIN_MIGRATION.md`**; resolve **journal** example email; align **STACK** / **gsd-project**; verify **`truongdq.com`** does not appear in **application source** or **deployment docs** except the migration note’s allowlist.

Purpose: **DOM-02** + remaining **DOM-01** items (nginx, preview).

Output: Updated nginx/preview/docs/planning; `bun run verify` green; `rg 'truongdq\.com'` scoped to intentional paths.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@nginx.conf
@PREVIEW_SETUP.md
@.planning/phases/06-domain-cutover/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nginx + preview docs</name>
  <files>nginx.conf, PREVIEW_SETUP.md</files>
  <action>
    1. Update `server_name` values to **`truongsoftware.com`**, **`www.truongsoftware.com`**, and **`preview.truongsoftware.com`** (or document chosen preview hostname consistently).

    2. Update `PREVIEW_SETUP.md` certbot/DNS examples to match; add a one-line **operator note** that DNS/TLS must be applied on the server.

    3. Update comments in `nginx.conf` that name the old domain.
  </action>
  <verify>
    <automated>grep -q truongsoftware.com nginx.conf PREVIEW_SETUP.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Migration doc + journal + planning refs</name>
  <files>docs/DOMAIN_MIGRATION.md, src/content/journal/2026-03-18-dry-kiss-yagni-technical-writing.md, .planning/codebase/STACK.md, .cursor/rules/gsd-project.md</files>
  <action>
    1. Create **`docs/DOMAIN_MIGRATION.md`**: state cutover from `truongdq.com` to `truongsoftware.com`; list any **content** files that still mention the old domain **on purpose** (if any remain after edit). If the journal code sample is updated to `noreply@truongsoftware.com`, say "examples updated"; if left historical, list path under **Intentional mentions**.

    2. **Either** update the journal markdown example email to a `truongsoftware.com` address **or** leave it and reference it in the migration doc (per 06-CONTEXT).

    3. Fix `.planning/codebase/STACK.md` and `.cursor/rules/gsd-project.md` lines that still say `site: https://truongdq.com` to **`https://truongsoftware.com`**.

    4. Run `rg 'truongdq\.com'` from repo root; remaining hits should be **only** migration note (and listed archived lines if kept), not random deployment text.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

</tasks>

<verification>
`bun run verify`; manual: read `docs/DOMAIN_MIGRATION.md`; spot-check `rg 'truongdq\.com'` output.
</verification>
