---
phase: 07-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - docs/REPO_HYGIENE.md
autonomous: true
requirements:
  - HK-01
user_setup: []
must_haves:
  truths:
    - "Unused dependencies are removed OR listed with rationale in docs/REPO_HYGIENE.md (Dependencies section)."
    - "bun run lint && bun run build succeed after changes (full verify recommended)."
  artifacts:
    - path: docs/REPO_HYGIENE.md
      provides: "Dependency audit notes and kept-optional packages"
---

<objective>
Run an **automated unused-dependency pass** (`depcheck`, `knip`, or equivalent), reconcile with **scripts/** and **astro** config, **remove** unused packages or **document** why they stay, and keep **`bun run verify`** green.

Purpose: **HK-01**.

Output: Updated `package.json` / lockfile; **`docs/REPO_HYGIENE.md`** started with a **Dependencies** subsection; verify passes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-housekeeping/07-CONTEXT.md
@.planning/phases/07-housekeeping/07-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unused dependency scan</name>
  <files>package.json, scripts/</files>
  <action>
    1. Run e.g. `bunx depcheck` or `bunx knip` from repo root (add knip/depcheck as **devDependency** only if needed and team-acceptable).

    2. For each unused hit: confirm no usage in `src/`, `scripts/`, `astro.config.mjs`, `eslint.config.mjs`.

    3. Remove confirmed-unused packages; run `bun install`.

    4. For **kept** questionable deps (e.g. `@astrojs/cloudflare`, forked `ai/react`): add a bullet under **docs/REPO_HYGIENE.md → Dependencies** with rationale.
  </action>
  <verify>
    <automated>bun run lint && bun run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Scripts review</name>
  <files>package.json</files>
  <action>
    1. Confirm every `scripts` entry in `package.json` still maps to a real script file or standard Astro command.

    2. Remove or document orphan script references; note in REPO_HYGIENE if a script is CI-only.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

</tasks>

<verification>
`bun run verify`
</verification>
