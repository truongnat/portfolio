---
phase: 07-housekeeping
plan: 02
type: execute
wave: 1
depends_on:
  - 07-01-PLAN.md
files_modified:
  - .gitignore
  - docs/REPO_HYGIENE.md
  - README.md
autonomous: true
requirements:
  - HK-02
user_setup: []
must_haves:
  truths:
    - ".gitignore covers local data, env, and secret key material patterns agreed in plan."
    - "docs/REPO_HYGIENE.md lists all *.astro.disabled routes with short rationale or removal note."
  artifacts:
    - path: docs/REPO_HYGIENE.md
      provides: "Ignore patterns + disabled routes inventory"
---

<objective>
Harden **`.gitignore`**, extend **`docs/REPO_HYGIENE.md`** (merge with 07-01 if same file) with **ignore rationale** and a **disabled routes** table, and link from **README** under Documentation.

Purpose: **HK-02**.

Output: Updated `.gitignore`; hygiene doc; optional README one-liner; `bun run verify` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.gitignore
@.planning/phases/07-housekeeping/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: gitignore patterns</name>
  <files>.gitignore</files>
  <action>
    1. Add safe patterns: e.g. `*.pem` (or `**/*.pem`), consider `.env.local`, `.env.*.local` if not already covered.

    2. Keep explicit `data/` for LanceDB; add one-line comment that `data/lancedb` is local index.

    3. Do not ignore required tracked assets (verify `git check-ignore -v` on sample paths if unsure).
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Disabled routes + README link</name>
  <files>docs/REPO_HYGIENE.md, README.md</files>
  <action>
    1. In **docs/REPO_HYGIENE.md**, add **Disabled routes** table for each `src/pages/**/*.astro.disabled` (path + one-line reason / future intent).

    2. If any `.disabled` file is obsolete, delete it and note "removed in Phase 7" in the doc — only if product agrees; default is **document, not delete**.

    3. Add README bullet next to DOMAIN_MIGRATION link: **Repo hygiene / ignored paths**.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

</tasks>

<verification>
`bun run verify`; `ls src/pages/**/*.disabled | wc -l` matches table rows.
</verification>
