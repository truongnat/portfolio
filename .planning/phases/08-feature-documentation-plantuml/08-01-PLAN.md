---
phase: 08-feature-documentation-plantuml
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/features/content-collections.puml
  - docs/features/content-collections.md
  - docs/features/page-shell-navigation.puml
  - docs/features/page-shell-navigation.md
  - docs/features/public-api-routes.puml
  - docs/features/public-api-routes.md
  - docs/features/semantic-search.puml
  - docs/features/semantic-search.md
  - docs/features/contact-telegram.puml
  - docs/features/contact-telegram.md
  - docs/features/payments-certificates.puml
  - docs/features/payments-certificates.md
autonomous: true
requirements:
  - DOC-02
user_setup: []
must_haves:
  truths:
    - "Six feature areas each have a sibling pair docs/features/<slug>.puml and docs/features/<slug>.md."
    - "Each .puml contains @startuml and @enduml with at least one diagram (component, sequence, or deployment)."
  artifacts:
    - path: docs/features/
      provides: "PlantUML sources and markdown companions per REQUIREMENTS catalog"
---

<objective>
Create **six** feature documentation pairs under `docs/features/`: for each catalogued area (content collections; page shell & navigation / command palette; public API routes; semantic search; contact/Telegram; payments & certificates), add one **`.puml`** with at least one valid diagram and one **`.md`** companion describing behavior, main file paths, and boundaries.

Purpose: **DOC-02**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-feature-documentation-plantuml/08-CONTEXT.md
@.planning/phases/08-feature-documentation-plantuml/08-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content collections — diagram + companion</name>
  <files>docs/features/content-collections.puml, docs/features/content-collections.md</files>
  <read_first>
    - src/content.config.ts
    - src/content/schemas.ts
    - .planning/phases/08-feature-documentation-plantuml/08-RESEARCH.md
  </read_first>
  <action>
    1. Add `docs/features/content-collections.puml` with a **component** diagram: collections `blog`, `journal`, `courses` → loaders → Zod schemas → pages (high level).

    2. Add `docs/features/content-collections.md` with: purpose; bullets listing `src/content.config.ts`, `src/content/schemas.ts`, and content directories; link or fenced reference to sibling `.puml`.
  </action>
  <acceptance_criteria>
    - `docs/features/content-collections.puml` contains `@startuml` and `@enduml`
    - `docs/features/content-collections.md` contains the string `content.config` and `content-collections.puml` or `./content-collections.puml`
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Page shell &amp; command palette — diagram + companion</name>
  <files>docs/features/page-shell-navigation.puml, docs/features/page-shell-navigation.md</files>
  <read_first>
    - src/layouts/BaseLayout.astro
    - src/components/CommandPalette.tsx
  </read_first>
  <action>
    1. Add `docs/features/page-shell-navigation.puml` (component or sequence) showing BaseLayout → CommandPalette (client island) → navigation/search affordances.

    2. Add companion markdown naming `BaseLayout.astro` and `CommandPalette.tsx`, persistence/hydration notes if relevant (`client:idle`).
  </action>
  <acceptance_criteria>
    - `docs/features/page-shell-navigation.md` contains `BaseLayout` and `CommandPalette`
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Public API routes — diagram + companion</name>
  <files>docs/features/public-api-routes.puml, docs/features/public-api-routes.md</files>
  <read_first>
    - .planning/phases/08-feature-documentation-plantuml/08-RESEARCH.md
    - src/pages/api/contact.ts
    - src/pages/api/search.ts
    - docs/REPO_HYGIENE.md
  </read_first>
  <action>
    1. Add `docs/features/public-api-routes.puml` grouping routes under **packages** matching `08-RESEARCH.md` § “`src/pages/api` inventory”: Core/integrations, Stripe, Crypto stub, Donations, Demo/stub UIs (22 `.ts` files total).

    2. Companion: table or bullet list of `/api/...` prefixes; explicitly call out stub/demo clusters and point to REPO_HYGIENE / CONCERNS for trust boundaries.
  </action>
  <acceptance_criteria>
    - `docs/features/public-api-routes.md` contains `/api/contact` and `src/pages/api`
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Semantic search — diagram + companion</name>
  <files>docs/features/semantic-search.puml, docs/features/semantic-search.md</files>
  <read_first>
    - src/pages/api/search.ts
    - scripts/index-content.ts
    - package.json
  </read_first>
  <action>
    1. Add `docs/features/semantic-search.puml` (deployment or sequence): index script → LanceDB → search API → client.

    2. Companion: document `data/lancedb`, that **`bun run journal:index`** runs `scripts/index-content.ts` (per `package.json`), embedding via `@xenova/transformers`, and production caveats from CONCERNS if needed.
  </action>
  <acceptance_criteria>
    - `docs/features/semantic-search.md` contains `search.ts` and `index-content`
    - `docs/features/semantic-search.md` contains `journal:index`
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 5: Contact / Telegram — diagram + companion</name>
  <files>docs/features/contact-telegram.puml, docs/features/contact-telegram.md</files>
  <read_first>
    - src/pages/api/contact.ts
  </read_first>
  <action>
    1. Add `docs/features/contact-telegram.puml` **sequence** diagram: form → POST `/api/contact` → Telegram HTTP.

    2. Companion: required env vars (`TELEGRAM_*`), failure modes (generic errors to client).
  </action>
  <acceptance_criteria>
    - `docs/features/contact-telegram.md` contains `TELEGRAM` or `telegram`
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 6: Payments &amp; certificates — diagram + companion</name>
  <files>docs/features/payments-certificates.puml, docs/features/payments-certificates.md</files>
  <read_first>
    - src/pages/api/stripe/webhook.ts
    - src/pages/api/certificate.ts
    - src/lib/certificate-generator.ts
  </read_first>
  <action>
    1. Add `docs/features/payments-certificates.puml` (component or sequence): Stripe webhook + create-payment; certificate issuance + HMAC; crypto stubs.

    2. Companion: trust boundaries (webhook secret, certificate signing), pointer to Phase 2/4 security requirements, crypto as stub where applicable.
  </action>
  <acceptance_criteria>
    - `docs/features/payments-certificates.md` contains `certificate` and `stripe`
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

</tasks>

<verification>
`bun run verify`  
Manual: `ls docs/features | wc -l` expects **12** lines (six `.puml` + six `.md`).
</verification>
