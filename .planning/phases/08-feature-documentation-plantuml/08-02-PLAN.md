---
phase: 08-feature-documentation-plantuml
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
files_modified:
  - docs/README.md
  - README.md
autonomous: true
requirements:
  - DOC-01
user_setup: []
must_haves:
  truths:
    - "docs/README.md is the documentation entry point and links every feature companion plus existing docs (DOMAIN_MIGRATION, REPO_HYGIENE)."
    - "Root README.md includes ## Feature architecture linking into docs/."
  artifacts:
    - path: docs/README.md
      provides: "Catalog of feature docs and top-level doc links"
---

<objective>
Add **`docs/README.md`** as the **entry point** for architecture docs: link **every** `docs/features/*.md` companion from plan 01, plus **`docs/DOMAIN_MIGRATION.md`** and **`docs/REPO_HYGIENE.md`**. Update **root `README.md`** with a **`## Feature architecture`** section pointing to `docs/README.md` (or `docs/`).

Purpose: **DOC-01**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-feature-documentation-plantuml/08-CONTEXT.md
@.planning/phases/08-feature-documentation-plantuml/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/README.md catalog</name>
  <files>docs/README.md</files>
  <read_first>
    - docs/DOMAIN_MIGRATION.md
    - docs/REPO_HYGIENE.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    1. Create `docs/README.md` with:
       - Title and one-paragraph purpose (architecture / feature map for contributors).
       - **Feature architecture** subsection with bullet links to each of:
         - `./features/content-collections.md`
         - `./features/page-shell-navigation.md`
         - `./features/public-api-routes.md`
         - `./features/semantic-search.md`
         - `./features/contact-telegram.md`
         - `./features/payments-certificates.md`
       - **Operations &amp; migration** (or similar) subsection linking `./DOMAIN_MIGRATION.md` and `./REPO_HYGIENE.md`.

    2. Ensure every link target path matches the filenames created in `08-01-PLAN.md`.
  </action>
  <acceptance_criteria>
    - `docs/README.md` exists and contains `./features/content-collections.md` and `./REPO_HYGIENE.md`
    - `docs/README.md` contains `./DOMAIN_MIGRATION.md`
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Root README Feature architecture section</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - docs/README.md
  </read_first>
  <action>
    1. In root `README.md`, add a section with heading exactly: `## Feature architecture`

    2. Body: 1–3 sentences + markdown link to **`docs/README.md`** (e.g. `[Feature architecture](./docs/README.md)`).

    3. Place after the main intro block or adjacent to existing **Documentation** section without duplicating the full feature list (pointer only).
  </action>
  <acceptance_criteria>
    - `grep -n '## Feature architecture' README.md` returns at least one line
    - `README.md` contains `./docs/README.md` or `docs/README.md`
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

</tasks>

<verification>
`bun run verify`  
`grep '## Feature architecture' README.md`
</verification>
